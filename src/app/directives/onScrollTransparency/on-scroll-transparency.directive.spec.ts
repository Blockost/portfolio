import { OnScrollTransparencyDirective } from './on-scroll-transparency.directive';

describe('TestHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OnScrollTransparencyNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
